# ApiDoc 覆盖进度跟踪模板

> 配合 `bin/kmcp coverage` 使用。目标：把 `https://openapi.open.kingdee.com/ApiDoc` 的 **16 业务领域 / 77 模块 / 标准动作集** 全部覆盖进 kingdee-mcp。

## 字段说明
- **领域 / 模块**：来自 `references/api-inventory-raw.json`（已全量抓取 16 领域 + 77 模块）。
- **动作覆盖**：每个模块的「保存/提交/审核/下推/查询…」是否已实现为 `@mcp.tool`。
- **状态**：未开始 / 进行中 / 已集成 / 有坑待修。

## 覆盖表（模板，按领域填充）

| 业务领域 | 模块 | 已实现工具 | 覆盖动作 | 状态 | 备注 |
|----------|------|-----------|----------|------|------|
| 供应链 | 采购管理 | `kingdee_query_purchase_orders` 等 | 查询/保存 | 部分 | 待补全下推/审核 |
| 供应链 | 销售管理 | `kingdee_query_sale_orders` | 查询 | 部分 | 二开 SAL_SaleOrder 坑 |
| 供应链 | 库存管理 | `kingdee_query_inventory` | 查询 | 部分 | — |
| 财务会计 | 应收款管理 | — | — | 未开始 | — |
| 财务会计 | 应付款管理 | — | — | 未开始 | — |
| 员工服务 | 人人报销 | — | — | 未开始 | 操作级已抓 98 项 |
| … | … | … | … | … | … |

## 统计口径
- 工具总数：`bin/kmcp list-tools`（当前约 97 个 `@mcp.tool`）。
- 覆盖度 = 已集成 ApiDoc 操作数 / ApiDoc 总操作数（操作级需逐模块补全抓取）。
- 每完成一个领域，在 `docs/mcp_optimization_notes.md` 按日期追加一条覆盖记录（用户长期习惯）。

## 抓取未尽项
- `api-inventory-raw.json` 中「操作」层仅在「员工服务 → 人人报销」完整（98 操作，标准动作集）。
- 其余 15 个领域的操作级清单需在浏览器中逐模块切换业务领域后抓取补全，才能给出精确覆盖率。
