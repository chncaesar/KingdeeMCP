# 金蝶 ApiDoc 1076 业务对象全量测试矩阵报告

- **报告日期**：2026-08-07
- **整理人**：温书成（文档与发布工程师）
- **数据来源**：`D:\AI\projects\kingdee-mcp\temp_test_matrix.json`（1076 对象全覆盖，含 domain/module/name/formid/status/test/note）
- **测试执行**：范探源产出 FormId 映射 → 严过关真机分层探测（本报告不重新研究、不重新跑测，严格以素材为准）
- **测试环境**：金蝶 K3Cloud `http://192.168.1.77/k3cloud/`，账套 `6979ac7e5959f4`（芯之蝶软件（深圳）有限公司），**全程只读，未改动任何数据**

---

## 1. 测试范围与方法

### 1.1 测试范围

- **对象规模**：ApiDoc 全量 **1076 个业务对象**（单据 / 基础资料 / 报表），覆盖 **16 业务领域、69 模块**。
- **对象来源**：`temp_test_matrix.json` 中 `objects` 数组逐项登记，含 `domain / module / name / formid / status / test / note` 七个字段。
- **附加对象**：另附 2 个账套侧二开单据 TRNV_Receipt / TRNV_PaymentSlip（不在 ApiDoc 1076 候选内，标注 `to_confirm`），合计 **1076 + 2 附加**。

### 1.2 测试方法（三层）

1. **FormId 映射**（范探源）：为 1076 个对象生成 FormId 映射 `formid_map`，**全部对象均有 FormId**，作为探测入参。
2. **全量查询链路探测**（严过关）：逐 FormId 真机调用 `ExecuteBillQuery` 探测（**12 并发**），并用 `QueryBusinessInfo` 复核；对 21 个未启用对象逐一调用验证优雅报错。
3. **动作链路抽样**（严过关）：对 5 个已启用对象抽样真机查询（`sampled_ok`），验证通用工具动作链路可用性。

### 1.3 判定规则

- **错误包装解析**：WebAPI 返回按「权限拒绝 `[[{'Result':{报错}}]]` vs 正常数据 `[[值,...]]`」区分，权限拒绝 / 崩溃不算「已启用可用」。
- **基础资料非 FID 主键**：基础资料类对象（如 BD_STOCK / BD_Department）主键非 FID，用默认 FID 报「FID 字段不存在」属**元数据差异而非不可用**，换用正确主键字段即正常。
- **大小写**：FormId / 字段 Key 按元数据大小写精确匹配（如 `FCustomerID` 非 `FCustId`）。

---

## 2. 测试结果总览

| 分层 | 数量 | 含义 | 验证方式 |
|------|-----:|------|----------|
| `queried_ok` | **1050** | 已启用对象，查询链路真机验证通过 | 逐 FormId ExecuteBillQuery + QueryBusinessInfo 复核 |
| `sampled_ok` | **5** | 已启用对象，动作链路抽样真机查询成功 | 5 个已启用对象抽样真机查询 |
| `code_checked` | **21** | 未启用对象，通用工具优雅报错验证 | 逐一调用，验证「业务对象不存在」类优雅错误 |
| `to_confirm` | **2** | TRNV 二开单（附加，不在 1076 内） | 三种途径实测均返回「业务对象不存在」，待账套人工确认 |
| **合计** | **1076** | 全量覆盖 | — |

- **已启用（enabled）合计**：1055 = 1050（queried_ok）+ 5（sampled_ok）。
- **未启用（disabled）合计**：21（code_checked）。
- **附加待确认**：2（to_confirm，TRNV 二开单）。

> 结论：**1076 个业务对象全量测试完成**——已启用 1055 个中查询链路真机验证 1050 + 动作链路抽样 5；未启用 21 个代码级优雅报错验证；另 2 个二开单标注待账套人工确认。

---

## 3. 21 个未启用对象明细（优雅报错验证）

> 以下 21 个对象在账套侧均为**「业务对象不存在 / 被删除」的优雅报错**，经通用工具逐一调用验证：**无权限拒绝、无崩溃**。

### 3.1 自定义 API 类（`*_CdpAuto`，12 个）

> 分布于总账 / 报表 / 预算 / 信用 / 发票 / 移动 / 安全 / 车间 / 零售门店协同等领域。

| 对象 FormId | 说明 |
|-------------|------|
| SGL_CdpAuto | 自定义 API（总账/报表域） |
| S7_CdpAuto | 自定义 API |
| SIV_CdpAuto | 自定义 API |
| SBM_CdpAuto | 自定义 API |
| S23_CdpAuto | 自定义 API |
| SCRE_CdpAuto | 自定义 API（信用域） |
| S21_CdpAuto | 自定义 API |
| S005056c00008968111e3b19c4083c463_CdpAuto | 自定义 API（发票域） |
| SSFC_CdpAuto | 自定义 API（车间域） |
| SSEC_CdpAuto | 自定义 API（安全域） |
| SMOB_CdpAuto | 自定义 API（移动域） |
| S5a24ac7372a681_CdpAuto | 自定义 API（零售门店协同域） |

### 3.2 PLM 业务类型（7 个）

| 对象 FormId | 说明 |
|-------------|------|
| 10105000000000000004F327BFCF3E30689 | 业务类型 C |
| 10204010000000000002454E6739EA4DADA | Autocad 父 |
| 1020402000000000000F2321EB8F4026069 | Autocad 子 |
| 1032100000000000000821E0922A5F90419 | EBOM1 |
| 103220000000000000014277D8C9AF45F4F | EBOM2 |
| PLM_PDM_1110000000000000000 | 超级 BOM 组 |
| PLM_PDM_1180000000000000000 | 立项申请单 |

### 3.3 ECC 电商分销（2 个）

| 对象 FormId | 说明 |
|-------------|------|
| ECC_DISTRIBUTOR | 网上分销商 |
| ECC_SHIPPINGTYPE | 物流方式 |

> 处理建议：未启用对象如需真机验证，需在**已启用对应模块的正式账套**中进行；当前账套未启用即优雅报错属预期行为，不影响通用工具可用性。

---

## 4. TRNV 风险项（to_confirm，附加 2 个）

- **对象**：`TRNV_Receipt`（二开收款单）、`TRNV_PaymentSlip`（二开付款单）。
- **背景**：TRNV 单**不在 ApiDoc 1076 候选内**（ApiDoc 仅标准版，不含二开），属本账套二开单据；历史文档曾称其存在。
- **实测结果**：账套侧**三种途径**——`ExecuteBillQuery` / `QueryBusinessInfo` / MCP 工具——**均返回「业务对象不存在」**。
- **风险判定**：可能已停用 / 已删除 / FormId 已变更，**需账套管理员在账套界面人工确认**。

> ⚠️ **建议**：账套管理员确认结果后，同步更新 `skills/kingdee-mcp-dev/references/ercustom-pitfalls.md` 中关于 TRNV_Receipt / TRNV_PaymentSlip 的描述（当前历史文档仍称存在，需与实际账套状态对齐）。

---

## 5. 已启用抽样结果（sampled_ok，5 个）

> 动作链路抽样真机查询成功，验证通用工具对真实业务对象的动作链路可用。

| 对象 FormId | 名称 | 返回行数 |
|-------------|------|---------:|
| BD_STOCK | 仓库 | 3 行 |
| AR_OtherRecAble | 其他应收单 | 1 行 |
| AP_OtherPayable | 其他应付单 | 2 行 |
| SAL_RETURNNOTICE | 销售退货通知单 | 1 行 |
| BD_Department | 部门 | 3 行 |

### 5.1 基础资料主键坑（元数据差异，非不可用）

- **现象**：`BD_STOCK` / `BD_Department` 等基础资料类对象主键**非 FID**，用默认 FID 查询报「FID 字段不存在」。
- **判定**：这是**元数据差异而非对象不可用**；换用正确主键字段后查询正常。
- **价值**：反向验证了**通用工具元数据纠错链路可用**——当入参字段与元数据不匹配时，工具能正确解析并引导纠错，而非报错崩溃。

---

## 6. 结论与建议

### 6.1 结论

- **1076 个业务对象全量测试完成**：
  - 查询链路真机验证 **1055 / 1076**（已启用 1050 queried_ok + 5 sampled_ok）；
  - 未启用 **21** 个完成代码级优雅报错验证（无权限拒绝、无崩溃）；
  - 附加 **2** 个 TRNV 二开单标注待人工确认（to_confirm）。
- **通用工具体系可用性验证通过**：逐 FormId ExecuteBillQuery 探测 + 错误包装解析 + 基础资料非 FID 主键纠错 + 未启用优雅报错，全链路只读验证，**未改动任何业务数据**。

### 6.2 建议

1. **未启用对象**：如需对 21 个未启用对象做真机查询验证，需切换到**已启用对应模块的正式账套**；当前优雅报错属预期，不阻塞通用工具交付。
2. **TRNV 人工确认**：请账套管理员在账套界面确认 `TRNV_Receipt` / `TRNV_PaymentSlip` 是否已停用 / 删除 / FormId 变更，确认后更新 `ercustom-pitfalls.md`。
3. **本轮测试性质**：全部**只读**操作，无数据写入 / 修改 / 删除，账套数据零变更。

---

*数据文件：`D:\AI\projects\kingdee-mcp\temp_test_matrix.json`（generated_at 2026-08-07 17:57:58）*
